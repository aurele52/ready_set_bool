###############################################################################
# PROJECT ROOT MAKEFILE
###############################################################################

EXOS = ex00 ex01 ex02 ex03 ex04 ex05 ex06 ex07 ex08 ex09 ex10 ex11
LIBS = utils common

.PHONY: all libs exos clean fclean re run help

###############################################################################
# BUILD EVERYTHING
###############################################################################

all: libs exos

###############################################################################
# BUILD LIBS
###############################################################################

libs:
	@for dir in $(LIBS); do \
		echo "===> Building $$dir"; \
		$(MAKE) -C $$dir; \
	done

###############################################################################
# BUILD EXERCISES
###############################################################################

exos:
	@for dir in $(EXOS); do \
		if [ -d $$dir ]; then \
			echo "===> Building $$dir"; \
			$(MAKE) -C $$dir; \
		fi \
	done

###############################################################################
# CLEAN OBJECT FILES
###############################################################################

clean:
	@for dir in $(LIBS) $(EXOS); do \
		if [ -d $$dir ]; then \
			echo "===> Cleaning $$dir"; \
			$(MAKE) -C $$dir clean; \
		fi \
	done

###############################################################################
# FULL CLEAN
###############################################################################

fclean:
	@for dir in $(LIBS) $(EXOS); do \
		if [ -d $$dir ]; then \
			echo "===> Full cleaning $$dir"; \
			$(MAKE) -C $$dir fclean; \
		fi \
	done

###############################################################################
# REBUILD EVERYTHING
###############################################################################

re: fclean all

###############################################################################
# RUN SPECIFIC EXO
###############################################################################
# Usage:
#   make run EX=ex03

run:
ifndef EX
	@echo "Usage: make run EX=ex03"
else
	$(MAKE) -C $(EX) run
endif

###############################################################################
# HELP
###############################################################################

help:
	@echo "Available targets:"
	@echo "  make          -> build libs + all exercises"
	@echo "  make clean    -> remove .cmo/.cmi everywhere"
	@echo "  make fclean   -> remove binaries + libs"
	@echo "  make re       -> full rebuild"
	@echo "  make run EX=ex03 -> run specific exercise"
